# Only needed when the API is on a different origin than the frontend.
# In development, vite.config.ts proxies /api to http://127.0.0.1:8000, so leave this unset.
# In production, set it to the deployed API's origin, with no trailing slash.
# VITE_API_URL=https://justpick-api.onrender.com
